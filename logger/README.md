# Logger

## Start the service

Create a `.env` file based on `template.env` and fill in your MQTT Broker's hostname.

### Development

```shell
npm install
npm run start:dev
```

### Production

```shell
docker-compose up -d
```
